# Как загрузить код на GitHub - Простая инструкция

## Способ 1: Через GitHub Desktop (САМЫЙ ПРОСТОЙ) ⭐

### Шаг 1: Скачай GitHub Desktop
1. Зайди на [desktop.github.com](https://desktop.github.com)
2. Скачай и установи GitHub Desktop
3. Войди в свой аккаунт GitHub

### Шаг 2: Клонируй репозиторий
1. Открой GitHub Desktop
2. Нажми "File" → "Clone repository"
3. Выбери вкладку "GitHub.com"
4. Найди свой репозиторий `my-bot-motiv`
5. Выбери папку, куда клонировать (например, `C:\Users\Никита\Desktop`)
6. Нажми "Clone"

### Шаг 3: Скопируй файлы
1. Скопируй ВСЕ файлы из папки `motiv bot` (кроме `venv` и `__pycache__`)
2. Вставь их в папку, куда клонировал репозиторий (должна быть папка `my-bot-motiv`)

### Шаг 4: Загрузи на GitHub
1. Открой GitHub Desktop
2. В левой панели увидишь все измененные файлы
3. Внизу напиши сообщение: "Добавлен код бота"
4. Нажми "Commit to main"
5. Нажми "Push origin" (кнопка сверху)

Готово! Код загружен на GitHub! 🎉

---

## Способ 2: Через командную строку (если Git установлен)

Открой PowerShell в папке проекта и выполни команды:

```powershell
# Перейди в папку проекта
cd "C:\Users\Никита\Desktop\motiv bot"

# Инициализируй Git (если еще не сделано)
git init

# Добавь все файлы
git add .

# Создай первый коммит
git commit -m "Добавлен код бота"

# Подключи свой репозиторий
git remote add origin https://github.com/Cruxcachikaa/my-bot-motiv.git

# Загрузи код
git branch -M main
git push -u origin main
```

Если попросит логин/пароль:
- Используй Personal Access Token вместо пароля
- Как создать токен: GitHub → Settings → Developer settings → Personal access tokens → Generate new token

---

## Способ 3: Через веб-интерфейс GitHub (самый простой, но долгий)

### Шаг 1: Подготовь файлы
1. Создай ZIP-архив всех файлов проекта (кроме `venv` и `__pycache__`)
2. Распакуй архив в отдельную папку

### Шаг 2: Загрузи на GitHub
1. Зайди на страницу репозитория: https://github.com/Cruxcachikaa/my-bot-motiv
2. Нажми "Add file" → "Upload files"
3. Перетащи все файлы из папки проекта
4. Внизу напиши: "Добавлен код бота"
5. Нажми "Commit changes"

---

## Какие файлы НЕ нужно загружать:

❌ НЕ загружай:
- Папку `venv/` (виртуальное окружение)
- Папки `__pycache__/`
- Файл `.env` (с секретными данными)
- Файл `data/motiv.db` (база данных)
- Папку `.git/` (если есть)

✅ Загружай:
- Папку `app/`
- Файлы `requirements.txt`, `pyproject.toml`
- Файлы `Dockerfile`, `docker-compose.yml`
- Файлы `README.md`, `DEPLOY.md` и другие `.md`
- Файл `.gitignore`
- Все остальные файлы проекта

---

## Проверка

После загрузки:
1. Обнови страницу репозитория на GitHub
2. Должны появиться все файлы проекта
3. Если видишь папку `app/` с кодом - всё правильно! ✅

---

## Если что-то пошло не так:

1. **Ошибка "repository not found"** - проверь название репозитория
2. **Ошибка авторизации** - создай Personal Access Token
3. **Файлы не загружаются** - проверь, что не превышен лимит размера файла (100MB на файл)

---

## Рекомендация

Для новичка лучше всего использовать **GitHub Desktop** - это самый простой способ! 🚀

