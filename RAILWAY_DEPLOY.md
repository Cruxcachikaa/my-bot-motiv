# Деплой на Railway - Пошаговая инструкция

## Шаг 1: Подготовка репозитория

### Если у тебя еще нет репозитория на GitHub:

1. Создай репозиторий на GitHub:
   - Зайди на [github.com](https://github.com)
   - Нажми "New repository"
   - Назови репозиторий (например, `motiv-bot`)
   - Выбери "Private" (если хочешь закрытый репозиторий)
   - Нажми "Create repository"

2. Загрузи код в репозиторий:

```bash
# В папке проекта
git init
git add .
git commit -m "Initial commit"
git branch -M main
git remote add origin https://github.com/твой-username/motiv-bot.git
git push -u origin main
```

**Важно:** Убедись, что `.env` файл в `.gitignore` (он уже там)!

---

## Шаг 2: Создание проекта на Railway

1. На странице "Новый проект" выбери **"Репозиторий GitHub"**
2. Авторизуйся через GitHub (если еще не авторизован)
3. Выбери свой репозиторий `motiv-bot`
4. Railway автоматически начнет деплой

---

## Шаг 3: Настройка переменных окружения

После создания проекта:

1. Перейди в настройки проекта (Settings)
2. Найди раздел "Variables" или "Environment Variables"
3. Добавь все переменные из `.env`:

```
BOT_TOKEN=твой_токен_бота
ADMIN_ID=твой_telegram_id
DATABASE_URL=sqlite+aiosqlite:///./data/motiv.db
TIMEZONE=Europe/Moscow
FORECAST_MODEL=SMA
FORECAST_WINDOW=7
FORECAST_COST_BEHAVIOR=extrapolate
CR_ALERT_THRESHOLD=0.0
EPL_DROP_THRESHOLD=0.3
LOG_LEVEL=INFO
```

**Важно:** 
- `BOT_TOKEN` - получи у [@BotFather](https://t.me/BotFather)
- `ADMIN_ID` - твой Telegram User ID (можно узнать у [@userinfobot](https://t.me/userinfobot))

---

## Шаг 4: Настройка деплоя

1. Перейди в раздел "Settings" → "Deploy"
2. Убедись, что:
   - **Build Command:** пусто (Railway определит автоматически)
   - **Start Command:** `python -m app.main`

Или создай файл `railway.json` в корне проекта (я уже создал его).

---

## Шаг 5: Запуск деплоя

1. Railway автоматически начнет деплой после подключения репозитория
2. Дождись завершения билда (обычно 2-3 минуты)
3. Проверь логи в разделе "Deployments" → выбери последний деплой → "View Logs"

---

## Шаг 6: Проверка работы

1. Открой Telegram бота
2. Отправь `/start`
3. Если бот отвечает - всё работает! 🎉

---

## Полезные команды

### Просмотр логов:
- В Railway: Deployments → выбери деплой → View Logs
- Или в терминале через Railway CLI: `railway logs`

### Перезапуск бота:
- В Railway: Deployments → выбери деплой → Redeploy

### Обновление кода:
```bash
git add .
git commit -m "Update"
git push
```
Railway автоматически задеплоит новую версию!

---

## Решение проблем

### Бот не запускается:
1. Проверь логи в Railway
2. Убедись, что все переменные окружения установлены
3. Проверь, что `BOT_TOKEN` и `ADMIN_ID` правильные

### Ошибка "Module not found":
- Railway должен автоматически установить зависимости из `requirements.txt`
- Если нет - добавь в Build Command: `pip install -r requirements.txt`

### База данных не сохраняется:
- Railway может очищать файлы при перезапуске
- Для продакшена лучше использовать PostgreSQL (можно добавить как сервис в Railway)

---

## Добавление PostgreSQL (опционально)

Если хочешь использовать PostgreSQL вместо SQLite:

1. В Railway создай новый сервис → "Database" → "PostgreSQL"
2. Railway автоматически создаст переменную `DATABASE_URL`
3. Обнови переменную `DATABASE_URL` в настройках бота
4. Установи драйвер PostgreSQL в `requirements.txt`:
   ```
   psycopg2-binary>=2.9.0
   ```
5. Измени `DATABASE_URL` на формат PostgreSQL:
   ```
   DATABASE_URL=postgresql://user:password@host:port/dbname
   ```

---

## Стоимость

### Hobby Plan (Бесплатный) - Рекомендуется для начала ✅

**Что включено:**
- $5 кредитов в месяц бесплатно
- 500 часов работы в месяц
- 512 MB RAM
- 1 GB дискового пространства
- Автоматические деплои из GitHub

**Хватит ли для одного пользователя?**
✅ **ДА!** Для Telegram бота с одним пользователем этого более чем достаточно:
- Бот потребляет очень мало ресурсов (обычно 50-100 MB RAM)
- 500 часов = ~20 дней непрерывной работы (бот может "спать" когда не используется)
- $5 кредитов обычно хватает на весь месяц

**Когда может не хватить:**
- Если бот работает 24/7 без остановки (может закончиться время)
- Если очень много данных в базе (может закончиться место)
- Если несколько пользователей активно используют бота

### Pro Plan ($20/месяц)
- Нужен только если:
  - Несколько активных пользователей
  - Нужна гарантированная работа 24/7
  - Нужно больше ресурсов

### Рекомендация
**Начни с бесплатного плана!** Он точно хватит для одного пользователя. Если в будущем понадобится больше - всегда можно перейти на Pro.

---

## Полезные ссылки

- [Railway Docs](https://docs.railway.app)
- [Railway Discord](https://discord.gg/railway)

